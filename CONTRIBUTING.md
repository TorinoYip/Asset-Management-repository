# 贡献约定

1. 从 `main` 创建功能分支，不直接覆盖 V1 参考图。
2. 指标口径变更必须同时更新 `docs/data/metric-dictionary.md`。
3. 信息架构或导览变更必须更新 draw.io 源稿，并重新导出预览。
4. 文件名使用英文 kebab-case；业务展示文本可使用中文。
5. 一个提交只包含一个明确主题，Pull Request 中说明变更范围、影响页面和验证方式。
